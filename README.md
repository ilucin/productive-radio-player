# productive-radio-player
Radio player via Productive API

Development:
- `npm install`
- `bower install`
- `gulp serve`

Production:
- `gulp build`

Inject token for Productive user somewhere in index.html:
`window.PRODUCTION_TOKEN = 'blabla';`
